import Activity from '../../containers/Activity'
import React from 'react'

const InputSettings = () => {
  return (
    <Activity
      pageTitle='Greenfield Input Settings'
      appBarTitle='Input Settings'
      style={{ maxHeight: '100%' }}
    >
    </Activity>
  )
}

export default InputSettings
